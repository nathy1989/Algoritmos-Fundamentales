/*Ejercicio 11:Devuelve el porcentaje de un número

Escribe una función que tome dos números (a y b) como argumento. Devolver b por ciento de a*/

const myFunction = (a, b) => {
    return (b / 100) * a
}

console.log(myFunction(100,50))

/*Interpretación: se utiliza formula mática de regla de 3 para determinar el porcentaje*/

